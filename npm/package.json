{
  "name": "xregistry-npm-bridge",
  "version": "1.0.0",
  "description": "xRegistry API wrapper for NPM registry",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker": "docker-compose up --build",
    "docker:dev": "docker-compose up"
  },
  "author": "xRegistry Team",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "express": "^4.18.2",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 